{
  "name": "@gnomon/core",
  "version": "0.0.0",
  "description": "Domain types, recurrence expansion, and ICS interop. No I/O.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "rrule-temporal": "^2.2.0",
    "temporal-polyfill": "^1.0.3"
  }
}
