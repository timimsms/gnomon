{
  "name": "@gnomon/server",
  "version": "0.0.0",
  "description": "Hono API, Drizzle schema, pg-boss jobs. Phase 2.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gnomon/core": "workspace:*",
    "jose": "^6.2.8"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
